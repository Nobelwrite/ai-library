# --- Python Virtual Environments ---
# Ignore the virtual environment directory. Track dependencies in requirements.txt.

.venv/
.pyenv/
.direnv/

# --- Python Cache & Compiled Files ---
# Ignore generated bytecode and cache folders.
__pycache__/
*.pyc
*.pyo
*.pyd
*.so
.Python

# --- Environment Variables ---
# CRITICAL: Ignore files containing environment variables and secrets!
# Store configuration securely, not in Git.
.env
*.env
.env.*
env.yaml

# --- Secrets / Credentials ---
# Ignore any other potential credential files (adjust names as needed)
secrets.json
credentials.json
*.pem
*.key

# --- IDE / Editor Configuration ---
# Ignore configuration files specific to development environments.

.idea/
# --- Operating System Files ---
# Ignore OS-specific metadata files.
.DS_Store
Thumbs.db
._*

# --- Build Artifacts & Packaging ---
# Ignore files generated during build or packaging processes.
build/
dist/
*.egg-info/
*.egg
wheels/
pip-wheel-metadata/
develop-eggs/
parts/
sdist/
*.manifest
*.spec

# --- Test Artifacts & Coverage ---
# Ignore files generated by testing frameworks and coverage tools.
.pytest_cache/
.coverage
*.coverage
coverage.xml
htmlcov/
nosetests.xml
pytestdebug.log
.tox/

# --- Databases ---
# Ignore local database files (if not meant to be versioned).
*.db
*.sqlite
*.sqlite3
dump.rdb

# --- Log Files ---
# Ignore application logs.
*.log
logs/
*.log.*

# --- Flask Instance Folder ---
# Flask's instance folder often holds configuration or databases
# that shouldn't be version controlled.
instance/
# --- Media Files (if uploaded locally during dev) ---
# Adjust if needed for your project structure
media/
uploads/
